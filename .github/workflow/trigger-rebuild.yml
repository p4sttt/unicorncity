name: Trigger Site Rebuild

on:
  push:
    branches: [main]

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger notegen rebuild
        run: |
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.NOTEGEN_DISPATCH_TOKEN }}" \
            "https://api.github.com/repos/${{ vars.NOTEGEN_REPO_URL }}/dispatches" \
            -d '{"event_type":"vault-updated"}'

